#!/bin/bash
alpine_launch () 
{ 
    DIR="$HOME"/alpine;
    FILE=$(find $DIR -iregex ".*alpine.*86_64.iso");
    qemu-system-x86_64 -m 512 -nic user,hostfwd=tcp::2222-:22 -boot d -cdrom $FILE -hda alpine.qcow2 -enable-kvm
}
alpine_launch "$@"
