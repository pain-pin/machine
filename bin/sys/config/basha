#!/usr/bin/bash

if [ "$#" -lt 1 ] || [ "$#" -gt 1 ]; then
    echo "Usage: $0 alias"
    exit 1
fi

F=$(which "$1")

if [ ! -f "$F" ]; then
	F="$BIN/$1"
	cp ~/.vim/templates/template.bashalias $F
fi
vim "$F"
chmod +x $BIN/$1

cd $BIN
gitaddcommit
cd -
src

exit 0

