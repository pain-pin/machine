#!/usr/bin/bash

if [ "$#" -lt 1 ] || [ "$#" -gt 1 ]; then
    echo "Usage: $0 alias"
    exit 1
fi

F=$(command -v -- "$1")

if [ -f "$F" ]; then
    vim "$F"
else
	vim "$BIN/$1"
fi

cd $BIN
gitaddcommit
cd -
src

exit 0

