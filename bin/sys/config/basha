#!/usr/bin/bash

if [ "$#" -lt 1 ] || [ "$#" -gt 1 ]; then
    echo "Usage: $0 alias"
    exit 1
fi

F=$(which "$1")

if [ ! -f "$F" ]; then
	F="$BIN/$1"
	cp ~/.vim/templates/template.my_aliases $F
fi
vim "$F"
chmod +x $F

cd $BIN
git pull
gitaddcommit
git push
cd -

exit 0

