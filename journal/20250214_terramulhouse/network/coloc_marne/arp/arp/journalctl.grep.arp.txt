250620
21:04:44
n
computer
/home/n/machine/journal/sysadmin/network/arp

250620-20:44:03-n@computer-~/machine/journal/networks/coloc_network-(main)
=> grep -i arp journalctl.grep.net.txt| sed s/[0-9]//g | cut -d\r -f 2- | sort | uniq -c | sort -rn | head -100
      6  kernel: bridge: filtering via arp/ip/iptables is no longer available by default. Update your scripts to load br_netfilter if you need this.

from chatgpt
💡 Summary
This warning is not an error, just an informational message.

If you don’t need to filter bridge traffic, you can ignore it.

If you rely on firewalls with Docker/VM bridges, follow the fix above.

Let me know if you'd like help verifying if your current iptables rules apply to bridged containers or VMs.

