#!/usr/bin/bash

if [ "$#" -lt 1 ] || [ "$#" -gt 3 ]; then
    echo "Usage: $0 key"
    echo "Usage: grep values after key=value"
    exit 1
fi

awk -v key="$1" '{
  for (i = 1; i <= NF; i++)
    if ($i ~ "^"key"=") {
      split($i, a, "=")
      print a[2]
    }
}' # | cut -d\  -f 1


exit 0

