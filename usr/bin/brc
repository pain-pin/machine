#!/bin/bash

set -e

F_NAME=".bashrc";
echo "ORIGINAL_PWD: $ORIGINAL_PWD"
cd "$SKEL_DIR/usr/home";
commit_if_modified "$F_NAME"
if [ $? -eq 0 ] ; then
	cp $F_NAME /home/$F_NAME;
	echo copied
fi
cd -;
source $HOME/$F_NAME
