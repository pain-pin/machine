#!/bin/bash
brc () 
{ 
    F_NAME=".bashrc";
	ORIGINAL_PWD=$(pwd);
    cd "$SKEL_DIR/usr/home";
	commit_if_modified "$F_NAME"
	if [ $? -eq 0 ] ; then
		cp $F_NAME /home/$F_NAME;
		echo copied
	fi
    cd $ORIGINAL_PWD;
    source $HOME/$F_NAME
}
brc "$@"
