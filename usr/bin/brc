#!/bin/bash
brc () 
{ 
    F_NAME=".bashrc";
	ORIGINAL_PWD=$PWD;
    cd "$SKEL_DIR/usr/home";
	commit_if_modified "$F_NAME"
	[[ "$?" == "0" ]] && { \
		cp $F_NAME /home/$F_NAME;
		echo copied
	}
echo "$? = " $?
    cd $ORIGINAL_PWD;
    source $HOME/$F_NAME
}
brc "$@"
